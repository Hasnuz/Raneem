export function PolicyPage({ eyebrow, title, intro, sections }: { eyebrow: string; title: string; intro: string; sections: { title: string; body: string; items?: string[] }[] }) {
  return <><section className="bg-mist py-16 md:py-20"><div className="container max-w-4xl"><p className="eyebrow">{eyebrow}</p><h1 className="section-title mt-4">{title}</h1><p className="mt-6 max-w-3xl text-lg leading-8 text-slate-600">{intro}</p><p className="mt-5 text-xs text-slate-400">Last updated: September 2026 · Professional UAE legal review is recommended before production launch.</p></div></section><article className="container max-w-4xl py-14 md:py-20">{sections.map(section=><section key={section.title} className="mb-10 border-b pb-10"><h2 className="text-2xl font-extrabold">{section.title}</h2><p className="mt-4 whitespace-pre-line leading-7 text-slate-600">{section.body}</p>{section.items&&<ul className="mt-4 list-disc space-y-2 pl-6 text-slate-600">{section.items.map(item=><li key={item}>{item}</li>)}</ul>}</section>)}</article></>;
}
